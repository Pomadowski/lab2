#include <cstdio>
#include <cstdlib>

int countdown(int a);
int countdown(int a){
	if(a==1){
		printf("1");
		return 0;
	}
	printf("%d \n", a);
	a--;
	a = countdown(a);
	
}

int main() {
	int a = 0;
	a = rand() % 100;
	a = countdown(a);
	return 0;
	
}
